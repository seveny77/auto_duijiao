"""项目使用的相机工作分辨率。"""

# 当前 500 万相机的固定工作全幅尺寸。
#
# 对焦流程以这里的尺寸恢复全幅并计算粗扫/精扫 ROI，不再从相机的
# WidthMax/HeightMax 节点读取最大可用尺寸。某些相机的 WidthMax 会
# 返回 2448，而现场实际工作画面配置为 2432×2048；将二者混用会导致
# 粗扫 4×4 时请求到不兼容的 612×512 ROI。
SENSOR_W = 2432
SENSOR_H = 2048
