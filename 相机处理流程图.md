# 自动对焦相机处理流程

```mermaid
flowchart TD
    START["相机初始化"] --> OPEN["open()\nSDK初始化\n枚举设备\n创建句柄"]
    OPEN --> BIN["set_binning(2,2)\n2048x1500"]
    BIN --> TRIG_OFF["TriggerMode=OFF\n连续取流模式"]
    TRIG_OFF --> CAP1["capture()\n采第1帧全图"]
    CAP1 --> DETECT["YOLO 检测目标\n获取 ROI 坐标"]
    DETECT --> SET_ROI["StopGrabbing\nset_roi(x,y,w,h)\n硬件开窗"]
    SET_ROI --> CALC_TARGETS["计算粗扫目标 index\nNCCSearch 初始化"]
    CALC_TARGETS --> TRIG_HW["TriggerMode=ON\nTriggerSource=Line0\n硬触发模式"]
    TRIG_HW --> START_CB["start_stream(callback)\n启动回调取流"]
    START_CB --> FLYCAP

    subgraph FLYCAP["飞拍阶段 PLC硬触发"]
        CB_LOOP["回调收到帧\nindex 判断"]
        CB_SKIP["跳过\n帧自动释放"]
        CB_EVAL["evaluate_image(img,ROI)\nTenengrad 评价"]
        CB_LOOP -->|"不在粗扫列表"| CB_SKIP
        CB_LOOP -->|"在粗扫列表"| CB_EVAL
        CB_SKIP --> CB_LOOP
        CB_EVAL --> CB_LOOP
    end

    FLYCAP --> STOP_CB["飞拍完成\nstop_stream()"]
    STOP_CB --> NCC["NCC 模板匹配\n估计偏移 dz"]
    NCC --> VERIFY["验证采图 ±2\n二次拟合"]
    VERIFY --> FIT_VERIFY["拟合点补评\n比较已知最优"]
    FIT_VERIFY --> RESULT["输出 best_index"]
    RESULT --> MOVE["PLC 移动到 best_index\n定点拍照确认"]
    MOVE --> DONE["流程结束"]

    style SET_ROI fill:#fff3e0,stroke:#f57c00
    style FLYCAP fill:#e3f2fd,stroke:#1976d2
    style NCC fill:#e8f5e9,stroke:#388e3c
```

## 关键阶段说明

| 阶段 | 取流状态 | 操作 |
|---|---|---|
| capture 单帧 | Start→采1帧→Stop | 全图采图用于检测 |
| set_roi | 已停止 | 改参数需停流 |
| 飞拍 | Start→持续→Stop | 回调取流，PLC硬触发 |

## 帧处理逻辑（回调内部）

```
on_frame(img):
    if frame_index in target_indices:     # 粗扫列表
        score = evaluator.evaluate_image(img, roi)  # ~0.5ms
        scores[frame_index] = score
    # 否则跳过，SDK自动释放帧缓冲
    frame_index += 1
```
