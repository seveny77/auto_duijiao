name: autofocus
channels:
  - defaults
dependencies:
  # Windows CPython 3.13 + PyTorch 2.7.1 CUDA can terminate inside a Qt worker
  # thread. Python 3.12 is the production runtime for GPU inference.
  - python=3.12
  - pip
